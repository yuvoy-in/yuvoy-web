# Yuvoy web — always use the PUBLIC npm registry (personal account isolation).
# Never route through any internal/work registry.
registry=https://registry.npmjs.org/
auto-install-peers=true
strict-peer-dependencies=false
