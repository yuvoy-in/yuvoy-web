# The contract is a byte-exact MIRROR of yuvoy-api, pinned by SHA. Formatting
# it makes it differ from the ref it is pinned to — a drift-check failure, and
# worse, a way to hide a real upstream change behind a formatting diff.
contracts/

.next
node_modules
pnpm-lock.yaml
coverage
next-env.d.ts
src/lib/api/schema.ts

# Machine-written session memory. The plugin rewrites these files outside
# Prettier's conventions, which fails `format:check` in CI on a file no
# human edits.
.memsearch
