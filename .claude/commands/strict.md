Implement the required changes with production-grade quality.

Requirements:

- No issues in dev or production environments
- Clean, minimal, and well-structured code
- No duplication or unnecessary complexity
- Strong separation of concerns

Constraints:

- Maintain current behaviour, UI, and animations exactly as they are
- Follow existing architecture unless an explicit improvement is requested

Output:

- Final implementation only — no summaries or explanations
