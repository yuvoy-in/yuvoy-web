Ensure the project builds successfully.

Steps:

- Identify and fix all build errors
- Continue iterating until the build passes cleanly (`pnpm build`)

Constraints:

- Do NOT alter existing behaviour, UI, or animations
- Do NOT refactor unrelated code
- Keep fixes strictly scoped to the errors

Output:

- Provide only the required fixes
